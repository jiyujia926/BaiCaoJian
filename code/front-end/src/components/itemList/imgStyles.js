import { makeStyles } from "@material-ui/core";

const imgStyles = makeStyles(()=>({
  content: {
    display: "flex",
    flexDirection: "row",
  },
  imgName: {
    
  },
  imgs: {
    maxWidth: "220px",
    maxHeight: "200px",
    width: "200px",
    height: "200px",
  },
}));

export default imgStyles;
