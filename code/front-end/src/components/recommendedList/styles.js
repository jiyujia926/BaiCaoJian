import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "300px",
    margin: "200px",
  },
  title: {
    fontSize: "18px",
  },
  item: {
    
  },
}));

export default useStyles;